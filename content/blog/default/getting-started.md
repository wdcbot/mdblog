---
title: "mdblog 完整使用指南"
date: 2026-01-10
tags: [教程, 文档]
pinned: true
---

欢迎使用 mdblog！这是一个轻量级的 Markdown 博客系统，无需数据库，简单易用。

---

## 一、部署到 Zeabur（推荐）

最简单的部署方式，5 分钟上线。

### 1. Fork 仓库

访问 [github.com/wdcbot/mdblog](https://github.com/wdcbot/mdblog)，点击右上角 **Fork** 按钮，将项目复制到你的 GitHub 账号。

### 2. 登录 Zeabur

访问 [zeabur.com](https://zeabur.com)，使用 GitHub 账号登录。

### 3. 创建项目

1. 点击「创建新项目」
2. 选择「共享集群」（有免费试用额度）
3. 地区选择「香港」（国内访问速度快）

### 4. 部署服务

1. 点击「添加服务」→「Git」
2. 授权并选择你 fork 的 mdblog 仓库
3. Zeabur 会自动检测 Go 项目并开始构建
4. 等待 2-3 分钟构建完成
5. 点击服务卡片 →「网络」→「生成域名」

### 5. 访问博客

- 前台：`你的域名`
- 后台：`你的域名/admin`
- 默认账号：`admin`
- 默认密码：`admin888`

> 💡 每次你 push 代码到 GitHub，Zeabur 会自动重新部署。

### 6. 配置持久化存储（重要）

默认情况下，每次重新部署会覆盖文章和数据。如果你想在后台写文章并保留数据，需要配置持久化存储：

1. 进入 Zeabur 控制台，点击你的 mdblog 服务
2. 点击「Volumes」标签
3. 添加以下 3 个 Volume：

| Volume ID | Mount Directory | 用途 |
|-----------|-----------------|------|
| `content` | `/app/content` | 文章数据 |
| `data` | `/app/data` | 评论、统计 |
| `uploads` | `/app/uploads` | 上传的图片 |

4. 每个都点击「Mount Volume」保存

**配置持久化后的行为：**
- 每次部署时，GitHub 仓库中的新文章会**自动合并**到 Volume（不会覆盖已有文章）
- 在后台写的文章会保留，不会丢失
- 如果 GitHub 和 Volume 有同名文件，保留 Volume 中的版本

> 💡 这样你可以同时在 GitHub 和后台管理文章，两边的内容会合并。

---

## 二、本地运行

如果你想在本地开发调试：

### 环境要求

- Go 1.21 或更高版本

### 运行步骤

```bash
# 克隆项目
git clone https://github.com/你的用户名/mdblog.git
cd mdblog

# 运行
go run main.go
```

访问地址：
- 前台：http://localhost:8080
- 后台：http://localhost:8080/admin

---

## 三、写文章

### 方式一：后台编辑器（推荐）

1. 登录后台 `/admin`
2. 点击「撰写新文章」
3. 使用 Markdown 编辑器写作
4. 支持：
   - 图片上传（点击按钮或直接粘贴）
   - 实时预览
   - 草稿保存
   - 文章置顶
5. 点击「保存」发布

### 方式二：直接创建文件

在 `content/blog/分类名/` 下创建 `.md` 文件：

```markdown
---
title: "文章标题"
date: 2026-01-10
tags: [标签1, 标签2]
---

正文内容，支持完整的 Markdown 语法...

## 二级标题

这是段落文字。

- 列表项 1
- 列表项 2

​```go
// 代码块
fmt.Println("Hello")
​```
```

---

## 四、文章属性

在文章开头的 `---` 之间设置元数据：

| 属性 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `title` | 字符串 | 文章标题（必填） | `"我的文章"` |
| `date` | 日期 | 发布日期（必填） | `2026-01-10` |
| `tags` | 数组 | 标签列表 | `[Go, 教程, 入门]` |
| `draft` | 布尔 | 草稿模式，不显示在前台 | `true` |
| `pinned` | 布尔 | 置顶文章 | `true` |

### 示例

```yaml
---
title: "Go 语言入门教程"
date: 2026-01-10
tags: [Go, 编程, 教程]
draft: false
pinned: true
---
```

---

## 五、分类管理

分类就是 `content/blog/` 下的文件夹名称。

### 目录结构

```
content/blog/
├── default/        # 默认分类
│   └── hello.md
├── tech/           # 技术分类
│   ├── go-intro.md
│   └── docker.md
└── life/           # 生活分类
    └── travel.md
```

### 创建新分类

**方式一：后台创建**
后台 → 分类管理 → 新建分类

**方式二：手动创建**
直接在 `content/blog/` 下创建新文件夹

---

## 六、独立页面

独立页面会自动显示在导航栏。

### 创建页面

在 `content/page/` 下创建 `.md` 文件：

```
content/page/
├── about.md     # 关于页面 → /page/about.html
├── links.md     # 友链页面 → /page/links.html
└── contact.md   # 联系页面 → /page/contact.html
```

### 页面格式

```markdown
---
title: "关于我"
---

页面内容...
```

---

## 七、配置说明

编辑 `config.yaml` 文件：

```yaml
# 服务器配置
server:
    port: 8080              # 端口号

# 管理员配置
admin:
    username: admin         # 登录用户名
    password: admin888      # 登录密码（请修改！）
    jwt_secret: xxx         # JWT 密钥（请修改！）

# 站点配置
site:
    title: 我的博客          # 站点标题
    description: 博客描述    # 站点描述
    author: 作者名           # 作者
    keywords: 博客,Go        # SEO 关键词
    base_url: https://xxx   # 站点地址
    
    # 外观设置
    logo: ""                # Logo 图片地址，留空显示文字
    logo_height: 36         # Logo 高度（像素）
    favicon: ""             # 网站图标
    accent_color: "#2563eb" # 主题色
    default_theme: auto     # 主题模式：light / dark / auto
    hero_title: "欢迎"      # 首页大标题（支持 HTML）
    
    # 功能开关
    comments_enabled: true      # 评论功能
    toc_enabled: true           # 文章目录
    reading_time_enabled: true  # 阅读时间估算
    
    # 页脚设置
    footer:
        copyright: "© 2026 My Blog"  # 版权信息
        icp: ""                       # 备案号
        links:                        # 社交链接
            - name: GitHub
              url: https://github.com/xxx
              icon: fa-brands fa-github
            - name: Twitter
              url: https://twitter.com/xxx
              icon: fa-brands fa-twitter

# 其他
posts_per_page: 10    # 每页文章数
theme: pure           # 主题名称
```

---

## 八、后台功能

访问 `/admin` 进入后台管理。

### 仪表盘
- 文章统计
- 评论统计
- 访问趋势图

### 文章管理
- 文章列表（支持搜索、筛选）
- 新建/编辑文章
- 批量删除
- 草稿管理

### 分类管理
- 查看所有分类
- 新建/重命名/删除分类

### 评论管理
- 查看所有评论
- 删除评论

### 系统设置
- 站点基本信息
- 外观设置
- 功能开关
- 页脚设置
- 数据备份/恢复

---

## 九、其他部署方式

### Docker 部署

```bash
docker-compose up -d
```

### 手动部署到服务器

```bash
# 编译
go build -o mdblog

# 运行
./mdblog
```

建议配合 systemd 或 supervisor 管理进程，使用 Nginx 反向代理。

### 静态部署（GitHub Pages）

如果不需要评论和后台功能：

```bash
go run main.go --build
```

生成的静态文件在 `public/` 目录。

---

## 十、目录结构

```
mdblog/
├── content/
│   ├── blog/           # 博客文章（按分类存放）
│   └── page/           # 独立页面
├── data/
│   ├── comments.json   # 评论数据
│   └── stats.json      # 访问统计
├── uploads/            # 上传的图片
├── themes/pure/        # 主题文件
├── admin/              # 后台模板
├── config.yaml         # 配置文件
└── main.go             # 程序入口
```

---

## 十一、常见问题

### 如何修改端口？

编辑 `config.yaml` 中的 `server.port`

### 如何修改管理员密码？

编辑 `config.yaml` 中的 `admin.password`，或在后台设置中修改

### 如何备份数据？

**方式一：后台备份**
后台 → 系统设置 → 备份数据

**方式二：手动备份**
备份以下目录：
- `content/` - 文章内容
- `data/` - 评论和统计
- `uploads/` - 上传的图片
- `config.yaml` - 配置文件

### 如何更换主题？

1. 在 `themes/` 下创建新主题文件夹
2. 修改 `config.yaml` 中的 `theme: 主题名`

### 图片上传失败？

检查 `uploads/` 目录是否有写入权限

### 搜索不工作？

搜索索引会在启动时自动构建，如果文章很多可能需要等待几秒

---

## 十二、获取帮助

- GitHub: [github.com/wdcbot/mdblog](https://github.com/wdcbot/mdblog)
- Issues: [提交问题](https://github.com/wdcbot/mdblog/issues)

欢迎 Star ⭐ 和 Fork！
